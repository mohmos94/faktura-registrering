import React from 'react';
import { Box, Typography } from '@mui/material';

const Header: React.FC = () => (
    <Box padding="10px" bgcolor="#f5f5f5" textAlign="center">
        <Typography variant="h4">Velkommen til Faktura System</Typography>
    </Box>
);

export default Header;
